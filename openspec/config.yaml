schema: spec-driven

context: |
  Tech stack: Next.js App Router, TypeScript, MySQL, Prisma 7, Tailwind CSS, Framer Motion, Zod, React Hook Form
  Mobile-first RTL Persian World Cup prediction campaign for wc.pishrosarmaye.com
  Follow KISS, YAGNI, SOLID. Never hardcode point values. Never allow predictions after match start.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
